{
    "title": "Amphi Pipeline Scheduler",
    "description": "Schedule Amphi pipelines to run at specified times using date, interval, or cron triggers",
    "properties": {
        "dbPath": {
            "type": "string",
            "description": "Custom path to the SQLite database file (defaults to ~/.amphi/scheduler.sqlite)"
        }
    },
    "additionalProperties": false,
    "type": "object"
}